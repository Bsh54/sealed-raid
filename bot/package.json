{
  "name": "sealed-raid-bot",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "esbuild index.mjs --bundle --platform=node --format=cjs --outfile=bundle.cjs",
    "start": "node bundle.cjs"
  },
  "dependencies": {
    "@inco/lightning-js": "^1.0.2",
    "dotenv": "^16.4.7",
    "viem": "^2.7.6"
  },
  "devDependencies": {
    "esbuild": "^0.24.0"
  }
}
